name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff pytest pandas numpy scikit-learn

      - name: Lint (ruff)
        run: ruff check src scripts apps tests

      - name: Run tests
        run: pytest tests/test_strategy.py tests/test_features.py -q
