# data/

Questa cartella contiene i dati locali. **Non versionare video né cache** (vedi `.gitignore`).

```
data/
├── samples/        # piccole clip di demo (metti solo campioni brevi o istruzioni di download)
├── annotations/    # etichette YOLO per il fine-tuning del detector
└── tracks/         # mappe 2D dei circuiti / punti di calibrazione omografia
```

## Video di demo
I diritti dei filmati F1 sono di FOM. Usa clip solo per dimostrazione tecnica, cita la fonte,
non ridistribuire. In alternativa usa onboard sintetici (simulatori/game) per materiale libero.

## Telemetria reale
Scaricata automaticamente da FastF1 in `.fastf1_cache/` alla prima esecuzione. Gratuita e ufficiale.
